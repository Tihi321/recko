export enum EGame {
  NumberOfAttempts = 6,
}

export enum EThemes {
  Dark = "Dark",
  Light = "Light",
}

export enum ELocalStorage {
  Settings = "recko/ts-settings",
}

export enum EModals {
  Info = "info",
  Settings = "settings",
}

export enum ELanguages {
  English = "en",
  Croatian = "hr",
}
